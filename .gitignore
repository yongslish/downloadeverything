node_modules/
runtime/downloads/
runtime/notes/
runtime/python/
runtime/funasr/
runtime/funasr-models/
runtime/transcriptions/
runtime/xhs-downloader/
runtime/secrets.env
runtime/setup.log
runtime/xhs-cookie.txt
runtime/xhs-api.log
bin/yt-dlp
# tesseract.js downloads these OCR model files to cwd at runtime (image-text
# OCR for xiaohongshu notes) — same category as the FunASR/yt-dlp runtime
# assets above, just rooted at the project root instead of runtime/.
*.traineddata
.DS_Store
__pycache__/
*.pyc

# Claude Code local/machine-specific config — but NOT .claude/skills/,
# which holds project skills (e.g. project-doctor) that should ship with
# the repo so any session opened here can use them.
.claude/settings.local.json
.claude/launch.json
.claude/skills/*/last-server.log

# Vite build output — regenerated via `npm run web:build`.
public/assets/
public/index.html
